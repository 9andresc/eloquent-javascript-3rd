// ECMAScript Modules

import ordinal from 'ordinal'
import { days as dayNames, months } from 'date-names'

export function formatDate(date, format) {
  /* ... */
}

export default ['Winter', 'Spring', 'Summer', 'Autumn']
